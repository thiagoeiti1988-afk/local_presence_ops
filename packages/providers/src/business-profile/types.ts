export interface PerformanceSnapshot {
  date: string; // ISO date (YYYY-MM-DD)
  views: number;
  searches: number;
  calls: number;
  websiteClicks: number;
  directions: number;
  bookings: number;
}
